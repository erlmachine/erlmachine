-module(gearbox_tracker).

-datasheet(<<"/tracker/erlmachine_tracker">>).
%% The next ending of .yaml will be added automatically
%% The root path will be substituted by os env or by default value /priv/datasheets (/priv is also allowed);

-export([]).
